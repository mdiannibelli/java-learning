public class Arrays {
    public static void main(String[] arrays) {
        int edad[] = {18,24,26,28}; // Declarando array con valores iniciales
        
        double peso[] = new double[5]; // Declaración y asignación de cantidad de elementos, desconocemos valores iniciales

        boolean vip[] = new boolean[4];

        String nombre[] = new String[6];

        peso[0] = 76.32;
        vip[3] = true;
        nombre[2] = "Andromeda";
        System.out.println(edad);
    }
}